# macOS системийн файл
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local session/temp
*.log
.env
.env.local
.env.*.local

# Secrets — хэрэв ирээдүйд n8n credentials г.м.-ийг repo-д хадгалах гэвэл энд тавина
secrets/
credentials/
*.key
*.pem

# Node (хэрэв build tool хэрэглэвэл)
node_modules/
dist/
build/
.cache/

# Backup файл (manual save хийсэн хуучин хувилбарууд)
*.bak
*-backup.*
*-old.*

# Sub-project өөрийн git repo-той тул bypass
m-event-website/.git/index.lock

# Temp / scratch
tmp/
scratch/

# Sub-project (өөрийн git-тэй marketing site)
m-event-website/
n8n-workflows-backup/

# VPS deploy — public репод БҮҮ оруул (backend workflow логик + DB холболт). Локал → scp-ээр серверт.
vps-deploy/

# Миграцийн дев-хэрэгсэл + дата — харилцагч/ажилтны PII (РД, утас, цалин, санхүү) агуулдаг.
# Public repo-д ОРОХГҮЙ. Локалд үлдэнэ. (2026-06-28: бүхэл фолдер track-аас гаргаж түүхээс цэвэрлэв.)
postgres-migration/

# Дата нөөц + дотоод тэмдэглэл — public репод ОРОХГҮЙ (санхүүгийн дата, стратегийн notes).
# Зэрэгцээ агент сесс эдгээрийг үүсгэдэг тул GitHub Desktop-д байнга гарч ирдэг байсан.
nomaad-backup/
*-backup-*.json
Нягтлан-цалин-build-vs-buy.md

# Бизнесийн дата snapshot (tools/biz-snapshot.js --json) — орлого/харилцагчийн тоо, public репод ОРОХГҮЙ.
biz-snapshot.json
